META_START
    name: alice
    parameters: 
    csockets: 
    epr_sockets: 
META_END

^b0 {type = LR}:
    vec<> = run_subroutine(vec<>) : subrt0

^b1 {type = host}:
    x = assign_cval() : 0

^b2 {type = LR}:
    vec<m> = run_subroutine(vec<>) : subrt1

^b3 {type = host}:
    return_result(m)

SUBROUTINE subrt0
    params:
    returns:
    uses: 0
    keeps: 0
    request: 
  NETQASM_START
    set Q0 0
    init Q0
    x Q0
  NETQASM_END

SUBROUTINE subrt1
    params:
    returns: M0 -> m
    uses: 0
    keeps:
    request: 
  NETQASM_START
    meas Q0 M0
    store M0 @output[0]
  NETQASM_END